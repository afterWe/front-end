import { styled } from 'styled-components';

export const Order = styled.li`
  padding: 0.8rem 0;
  font-size: 18px;
`;

export const OrderWrap = styled.li`
  margin-top: 1.5rem;
  overflow-y: auto;
  height: 500px;
`;
